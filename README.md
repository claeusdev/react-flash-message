## A very simple notifications component for React applications.

It looks something like this.

```JS
  <Toast
    type={type}
    message={message}
    duration={3000}
    active={active}
    setActive={setActive}
    position={position}
  />
```
